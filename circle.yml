dependencies:
  pre:
    - go get github.com/mattn/gom
  override:
    - gom install
    - npm install
    - npm install -g gulp
  post:
    - gom build server.go
    - gulp build-reelase
    - gulp sass-release
database:
  pre:
    - mysql -u $DB_USER -e "CREATE DATABASE $DB_TEST_NAME;"
  override:
    - gom exec goose -env test up

test:
  override:
    - gom exec ginkgo -r ./