linter:
  golangci_lint:
    disable:
      - unused
      - bodyclose
  eslint:
    npm_install: true
    root_dir: ./assets
    dir:
      - js
    config: '.eslintrc.json'
    ext: '.ts,.tsx'
    ignore-path: .eslintignore
