linter:
  golangci_lint:
    disable-all: true
    enable:
      - golint
      - gosec
      - govet
  eslint:
    npm_install: true
    root_dir: ./assets
    dir:
      - js
    config: '.eslintrc.json'
    ext: '.ts,.tsx'
    ignore-path: .eslintignore
