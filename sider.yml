linter:
  golangci_lint:
    target: server/...
    skip-dirs:
      - ./config
  eslint:
    npm_install: true
    root_dir: ./assets
    dir:
      - js
    config: '.eslintrc.json'
    ext: '.ts,.tsx'
    ignore-path: .eslintignore
