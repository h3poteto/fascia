linter:
  golangci_lint:
    disable-all: true
    enable:
      - golint
      - gosec
      - govet
    skip-dirs:
      - config
  eslint:
    npm_install: true
    root_dir: ./assets
    dir:
      - js
    config: '.eslintrc.json'
    ext: '.ts,.tsx'
    ignore-path: .eslintignore
