linter:
  golangci_lint:
    skip-dirs:
      - config
  eslint:
    npm_install: true
    root_dir: ./assets
    dir:
      - js
    config: '.eslintrc.json'
    ext: '.ts,.tsx'
    ignore-path: .eslintignore
